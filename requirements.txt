# Test dependencies — packages required for CI unit tests.
# Module runtime dependencies belong in each module's config.yml under
# dependencies.python rather than here.
pyyaml
requests
pypubsub
paho-mqtt
